interface:
  display_name: "External Dataset Curated Import"
  short_description: "Top-level workflow for BAFU, USLCI, and other structured LCA package imports"
  default_prompt: "Use $external-dataset-curated-import for packaged LCA data. Keep conversion local, then before remote work require CLI OAuth status/doctor-auth, separate account sessions, and human browser login; never request credentials, codes, tokens, or API keys. Preserve queue, dry-run, commit, closeout, and readback evidence."
