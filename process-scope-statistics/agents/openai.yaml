interface:
  display_name: "Process Scope Statistics"
  short_description: "Count repeatable coverage stats for remote process snapshots"
  default_prompt: "Use $process-scope-statistics through the CLI OAuth session. Check auth status first, hand login to the human if required, keep account sessions separate, and never request a password, code, token, or legacy API key."
