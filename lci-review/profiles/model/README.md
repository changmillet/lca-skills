# model profile (planned)

Status: **not implemented yet**.

Next step suggestion:
1. Define model-level review scope and required inputs.
2. Implement `profiles/model/scripts/run_model_review.py`.
3. Route `--profile model` in `scripts/run_review.py`.
