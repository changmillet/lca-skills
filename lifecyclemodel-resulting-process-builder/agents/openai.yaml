interface:
  display_name: "Lifecycle Model Resulting Process Builder"
  short_description: "Build resulting processes via the unified CLI"
  default_prompt: "Use $lifecyclemodel-resulting-process-builder through the pinned CLI. Keep the default flow local; before an explicit remote lookup check OAuth status, hand login to the human, keep account sessions separate, and never request credentials, codes, tokens, or API keys."
