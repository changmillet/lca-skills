interface:
  display_name: "Lifecycle Model Hybrid Search"
  short_description: "Debug and tune lifecycle model search requests"
  default_prompt: "Use $lifecyclemodel-hybrid-search to validate lifecycle model query, filter, and response behavior through the pinned published CLI 0.1.1 via pnpm unless an exact local CLI override is explicitly selected."
