interface:
  display_name: "Process Hybrid Search"
  short_description: "Debug and tune process hybrid search requests"
  default_prompt: "Use $process-hybrid-search to validate a process query, filter, and response through the pinned CLI OAuth session. Check `tiangong-lca auth status --json` first; if login is required, hand the trusted terminal to the human and never request a password, code, token, or legacy API key."
