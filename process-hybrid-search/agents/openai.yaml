interface:
  display_name: "Process Hybrid Search"
  short_description: "Debug and tune process hybrid search requests"
  default_prompt: "Use $process-hybrid-search to validate process query, filter, and response behavior through the pinned published CLI 0.1.1 via pnpm unless an exact local CLI override is explicitly selected."
