interface:
  display_name: "LCA Publish Executor"
  short_description: "Publish LCA artifact bundles through CLI verification gates"
  default_prompt: "Use $lca-publish-executor through CLI OAuth. Require doctor-auth before publish, keep account sessions separate, hand login to the human, never request credentials/codes/tokens/API keys, then read publish and verification reports and stop on blockers."
