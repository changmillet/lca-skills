interface:
  display_name: "Flow Hybrid Search"
  short_description: "Debug and tune flow hybrid search requests"
  default_prompt: "Use $flow-hybrid-search to validate a flow query request, filter, and response behavior."
