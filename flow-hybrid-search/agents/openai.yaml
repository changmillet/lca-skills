interface:
  display_name: "Flow Hybrid Search"
  short_description: "Debug and tune flow hybrid search requests"
  default_prompt: "Use $flow-hybrid-search to validate a flow query request, filter, and response through the pinned CLI OAuth session. Check `tiangong-lca auth status --json` first; if login is required, hand the trusted terminal to the human and never request a password, code, token, or legacy API key."
