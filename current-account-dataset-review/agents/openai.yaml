interface:
  display_name: "Current Account Dataset Review"
  short_description: "Run CLI-backed current-account dataset review, reference, and verification gates"
  default_prompt: "Use $current-account-dataset-review through the CLI OAuth session. Check auth status first, require doctor-auth before commit, keep separate private sessions per account, hand browser login to the human, and never request a password, code, token, or legacy API key."
